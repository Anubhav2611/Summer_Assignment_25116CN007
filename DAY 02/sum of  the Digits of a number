#include<stdio.h>
int main(){
    int n,sum=0,rem;
    printf("input n:");
    scanf("%d",&n);
    while(n>0){
        rem=n%10;
        sum+=rem;
        n/=10;
    }printf("sum of  the the digit of a number is:%d",sum);
        return 0;
}
